# not Native, so we use List
# we use numpy & array module to create array Data structure
# stores elements in contiguous memory without the help of pointers, reducing memory overhead.
# Same data type
